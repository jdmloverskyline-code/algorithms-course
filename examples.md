# Приклади алгоритмів з розбором

## Приклад 1: Знаходження коренів квадратного рівняння

**Задача:** Знайти корені рівняння ax² + bx + c = 0

**Алгоритм (словесний):**
1. Ввести коефіцієнти a, b, c
2. Якщо a = 0, то це не квадратне рівняння — повідомити про помилку
3. Обчислити дискримінант D = b² - 4ac
4. Якщо D < 0 — коренів немає
5. Якщо D = 0 — один корінь: x = -b / (2a)
6. Якщо D > 0 — два корені: x₁ = (-b + √D) / (2a), x₂ = (-b - √D) / (2a)
7. Вивести результат

**Програма на Python:**
```python
import math

print("=== Розв'язання квадратного рівняння ===")
a = float(input("Введіть коефіцієнт a: "))
b = float(input("Введіть коефіцієнт b: "))
c = float(input("Введіть коефіцієнт c: "))

if a == 0:
    print("Це не квадратне рівняння (a = 0)")
else:
    D = b**2 - 4*a*c
    print(f"Дискримінант D = {D}")
    
    if D < 0:
        print("Дійсних коренів немає")
    elif D == 0:
        x = -b / (2*a)
        print(f"Один корінь: x = {x}")
    else:
        x1 = (-b + math.sqrt(D)) / (2*a)
        x2 = (-b - math.sqrt(D)) / (2*a)
        print(f"Два корені: x₁ = {x1:.2f}, x₂ = {x2:.2f}")
```

**Тестування:**
- a=1, b=-5, c=6 → x₁=3, x₂=2 ✓
- a=1, b=2, c=1 → x=-1 ✓
- a=1, b=0, c=1 → коренів немає ✓

---

## Приклад 2: Пошук елемента в масиві (лінійний пошук)

```python
def лінійний_пошук(список, шуканий):
    """
    Шукає елемент у списку.
    Повертає індекс або -1, якщо не знайдено.
    """
    for i in range(len(список)):
        if список[i] == шуканий:
            return i
    return -1

# Тест
оцінки = [8, 11, 6, 9, 12, 7, 10]
значення = 12

результат = лінійний_пошук(оцінки, значення)
if результат != -1:
    print(f"Елемент {значення} знайдено на позиції {результат}")
else:
    print(f"Елемент {значення} не знайдено")
```

---

## Приклад 3: Обробка рядків

```python
# Підрахунок кількості голосних у слові
def підрахунок_голосних(слово):
    голосні = "аеєиіїоуюяАЕЄИІЇОУЮЯ"
    кількість = 0
    for літера in слово:
        if літера in голосні:
            кількість += 1
    return кількість

слово = input("Введіть слово: ")
print(f"Кількість голосних у слові '{слово}': {підрахунок_голосних(слово)}")
```

---

## Приклад 4: Рекурсія — Факторіал

```python
def факторіал(n):
    """Обчислює n! рекурсивно."""
    if n == 0 or n == 1:  # Базовий випадок
        return 1
    return n * факторіал(n - 1)  # Рекурсивний виклик

# Демонстрація
for i in range(8):
    print(f"{i}! = {факторіал(i)}")
```

**Трасування для факторіал(4):**
```
факторіал(4)
  = 4 × факторіал(3)
  = 4 × 3 × факторіал(2)
  = 4 × 3 × 2 × факторіал(1)
  = 4 × 3 × 2 × 1
  = 24
```
